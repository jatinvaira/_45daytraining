public class constructorjava {
    
    // special type of method which is used ffor initionisilng the member variables and perform any operation at the time of object creation

    // rules
    // Constructor name and class name should be always same
    // constructor should not have any return type
    // constructor should always be public
    // there can only one instance of a class, so no need for multiple constructors
    // dot operatorr is used to call a meathod but a keyword new operator is used to call the constructor
    // meathod can have a return type but no return type in constructor


    // types of constructor
    // zero argument constructor
    // parameterized constructor
    // copy constructor
    // default constructor
    // implicit constructor
    // explicit constructor
    


}
